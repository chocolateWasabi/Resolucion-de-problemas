#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

const int criteria[] = {0, 0, 1, 7, 4, 5, 9, 8};

long long 

void segment(int n, int k, int& m, vector<int>& possible){
    if(k < 2){
        m = current > 
        return;
    }
}

int main(){
    int n;
    cin >> n;

    int m = 0;
    vector<int> possible;
    segment(n, 7, m, possible);
    return 0;
}